/// The current version of `SwiftL10nCore`. Embedded in cache entries so that
/// entries produced by an older version of the library are discarded automatically.
public enum SwiftL10nCoreVersion {
    public static let current = "0.6.2"
}
